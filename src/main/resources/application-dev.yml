server:
  port: 8080
   
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://39.105.39.82:3306/kgconline?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver
   
mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.example.entity
   
#showSql
logging:
  level:
    com:
      example:
        mapper : debug